


//  function writePassword() {
//     var password = generatePassword(); 
//     var passwordText = document.querySelector("#password");

// test to generate the box pop up to ask for characters number 
var passLength = prompt("Please enter password length between 8 and 128 characters");
// prompt to confirm Uppercase to be included 
var upperCase = confirm("Would you like to include UppperCase Letters?");
// prompt to confirm Lowercase to be included
var numbers = confirm("Would you like to include LowerCase Letters?");
// prompt to include numbers
var numbers = confirm("Would you like to include Numbers?");
// prompt to include special characters
var specialCharacters = confirm ("Would you like to include Special Charcters");



// pseudocode: 
// answer above questions
// system will store responses  
// the responses would create an Array
// based on the responses using Math Random the system will geneate a writePassword
// Math Random

// If X characters between 8 and 128 
// if upperCase, lowercase, numbers, special characters
// if lowercase, number, special characters
// if number specialcharchters
// if specialcharcters

// system will generate the writePassword. function name(params) {
    
// }
// For Loop for the Math Random code: 
// for (var i = 0; i < enter; i++) {
//     var pickChoices = choices[Math.floor(Math.random() * choices.length)];
//     password.push(pickChoices);
